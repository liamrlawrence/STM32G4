#pragma once

#include <cstdint>


/// The maximum value of a u64.
#define U64_MAX	UINT64_MAX


typedef uint8_t u8;
typedef uint16_t u16;
typedef uint32_t u32;

typedef uint_fast8_t uf8;   // TODO: Replace with u32 / size_t

typedef volatile u32 vu32;

